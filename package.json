{
  "name": "eslint-plugin-lodash-to-native",
  "version": "0.0.1",
  "description": "ESLint plugin for replacing Lodash _.map with native Array#map",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests --recursive"
  },
  "dependencies": {
  },
  "devDependencies": {
    "eslint": "~6.5.0",
    "mocha": "^3.1.2"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "license": "ISC"
}
